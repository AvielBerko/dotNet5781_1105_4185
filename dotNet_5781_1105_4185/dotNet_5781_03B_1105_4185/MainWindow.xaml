<Window x:Class="dotNet_5781_03B_1105_4185.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:dotNet_5781_03B_1105_4185"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Grid Margin="10">
		<ListView ItemsSource="{Binding}">
			<ListView.View>
				<GridView x:Name="busgrigview">
					<GridViewColumn Header="Bus Registration" DisplayMemberBinding="{Binding Registration}" />
					<GridViewColumn Header="Date"  DisplayMemberBinding="{Binding Registration.Date}" />
					<GridViewColumn Header="Fuel Left (%)" DisplayMemberBinding="{Binding FuelLeft}" />
					<GridViewColumn Header="Treatment Needed?" DisplayMemberBinding="{Binding TreatmentNeeded}" />
					<GridViewColumn Header="Status"  DisplayMemberBinding="{Binding Status}" />
					<!--<GridViewColumn Width="50">
						<GridViewColumn.CellTemplate>
							<DataTemplate>
								<Button   Content="תדלוק"  Margin="0"
                                VerticalAlignment="Center"
                                Click="Button_Click" />
							</DataTemplate>
						</GridViewColumn.CellTemplate>
					</GridViewColumn>-->
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</Window>
